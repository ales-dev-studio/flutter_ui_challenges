import 'package:flutter/material.dart';

class ShowCaseDemoScreen extends StatelessWidget {
  const ShowCaseDemoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Show case demo'),
      ),
    );
  }
}
